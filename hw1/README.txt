README
COPY homework

This C++ program uses ios::binary rdbuf(); functionality to copy the 
binary values of any file to a new source.
from.ogv-->to.ogv (arbitrary non-text file, values can be changed)